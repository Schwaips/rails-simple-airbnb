<h1>All flats</h1>

<%= link_to  "Create a new flat", new_flat_path %>

<table class="table">
   <tr>
    <th>Name</th>
    <th>Address</th>
    <th>Description</th>
    <th>Price per night</th>
    <th>Number of guests</th>
  </tr>
    <% @flats.each do |flat| %>
  <tr>
    <td><%= link_to flat.name, flat_path(flat) %></td>
    <td> <%=flat.address  %> </td>
    <td> <%= flat.description %> </td>
    <td> <%= flat.price_per_night %> €</td>
    <td><%= flat.number_of_guests %></td>
    <td><%= link_to "Voir", flat_path(flat)  %></td>
    <td><%= link_to "Delete", flat_path(flat), method: :delete, data:{ confirm: "Delete the flat ?" }  %></td>
    <td><%= link_to "Edit", edit_flat_path(flat)  %></td>
  </tr>
  <% end %>
</table>
